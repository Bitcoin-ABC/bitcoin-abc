# Bitcoin ABC ${PROJECT_VERSION} Release Notes

Bitcoin ABC version ${PROJECT_VERSION} is now available from:

  <https://download.bitcoinabc.org/${PROJECT_VERSION}/>

This is a maintenance release with no user-visible change.
