#!/bin/sh

mkdir -p "${NATIVE_BUILD_DIR}"
cd "${NATIVE_BUILD_DIR}"
${CMAKE_COMMAND} -G${CMAKE_GENERATOR} "${CMAKE_SOURCE_DIR}" \
    -D__IS_NATIVE_BUILD=1 \
    -DCMAKE_MAKE_PROGRAM=${CMAKE_MAKE_PROGRAM} \
    ${ARGS}
